# ===========================================
# SIUJI LANDING PAGE CMS - ENVIRONMENT CONFIG
# ===========================================

# Environment
NODE_ENV=development

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
POSTGRES_DB=siuji
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_postgres_password

# Database Configuration for Local Development
# Use this when running PostgreSQL locally
# DATABASE_URI=postgres://postgres:your_password@localhost:5432/siuji

# Database Configuration for Docker
# Uncomment the line below if using Docker Compose
# DATABASE_URI=postgres://postgres:your_password@db:5432/siuji

# ===========================================
# PAYLOAD CMS CONFIGURATION
# ===========================================
# Generate a secure 32-character secret key with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# IMPORTANT: Change this to a unique secret for production!
PAYLOAD_SECRET=your_32_character_secret_key_here

# ===========================================
# SERVER CONFIGURATION
# ===========================================
# Main server URL where the CMS backend runs
SERVER_URL=http://localhost:3000

# API base URL for frontend consumption
API_BASE_URL=http://localhost:3000/api

# Frontend application URL (Vue.js development server)
FRONTEND_URL=http://localhost:5173

# Port for the backend server
PORT=3000

# ===========================================
# CORS & SECURITY CONFIGURATION
# ===========================================
# CORS: Allowed origins for cross-origin requests
# Add your frontend URLs, development tools, and network IPs
CORS_ORIGINS=http://localhost:5173,http://localhost:8080,http://127.0.0.1:5173,http://127.0.0.1:8080

# CSRF: Allowed origins for CSRF protection
# Should match CORS_ORIGINS for development
CSRF_ORIGINS=http://localhost:5173,http://localhost:8080,http://127.0.0.1:5173,http://127.0.0.1:8080

# ===========================================
# FILE UPLOAD CONFIGURATION
# ===========================================
# Maximum file size in bytes (10MB = 10485760 bytes)
MAX_FILE_SIZE=10485760

# Directory for uploaded files
UPLOAD_DIR=./uploads

# ===========================================
# SECURITY CONFIGURATION
# ===========================================
# Cookie security settings for development
COOKIE_SECURE=false
COOKIE_SAME_SITE=lax
COOKIE_DOMAIN=.local

# ===========================================
# EMAIL CONFIGURATION
# ===========================================
# Email service provider (gmail, outlook, etc.)
EMAIL_SERVICE=gmail

# SMTP server configuration
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false

# Email credentials
# For Gmail: Use App Password instead of regular password
# Go to: Google Account → Security → 2-Step Verification → App Passwords
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password_here

# Default sender information
EMAIL_FROM=noreply@yourdomain.com
EMAIL_TO=admin@yourdomain.com

# ===========================================
# ADMIN NOTIFICATION SETTINGS
# ===========================================
# Email address for admin notifications (contact form submissions, etc.)
ADMIN_EMAIL=admin@yourdomain.com

# Company name for email templates
COMPANY_NAME=Your Company Name

# ===========================================
# DEVELOPMENT NOTES
# ===========================================
# 1. Copy this file to .env and fill in your actual values
# 2. Never commit .env file to version control
# 3. Generate a unique PAYLOAD_SECRET for production
# 4. Use strong passwords for database and email
# 5. For network access, add your IP to CORS_ORIGINS and CSRF_ORIGINS
#    Example: http://192.168.1.100:3000,http://192.168.1.100:5173
